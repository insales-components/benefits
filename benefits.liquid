{% if block_lists.benefits.blocks.size > 0 %}

<div class="container">
  <div class="benefits">
    <div class="benefits-title">
      Преимущества
    </div>
    <div class="row">

      {% for block in block_lists.benefits.blocks %}
      {% assign tag = 'div' %}
      {% assign attr = '' %}
      {% if block.link.size > 0 %}
        {% assign tag = 'a' %}
        {% assign attr = 'href="' | append: block.link | append: '"' %}
      {% endif %}
      <div class="cell-fifth">
        <{{ tag }} {{ attr }} class="benefits-item">
          <div class="benefits-icon row flex-bottom flex-center">
            <img src="http://fakeimg.pl/300x300/d5d5d5/" alt="">
            <!-- <img src="{{ block.image }}" alt=""> -->
          </div>
          <div class="benefits-text">
            {{ block.title }}
          </div>
        </{{ tag }}>
      </div>
      {% endfor %}

    </div>
  </div>
</div>

{% endif %}
